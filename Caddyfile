{
    email help@cybermonkey.net.au  # Email for Let's Encrypt 
}

cybermonkey.dev {
    reverse_proxy http://100.65.138.128:8080 {
    }

    log {
        output file /var/log/caddy/hecate-access.log
        format json
    }

    tls {
        on_demand                 # Generate TLS certificates as needed
    }
}